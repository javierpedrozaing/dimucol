<%= form_for(@contratos_plan) do |f| %>
  <% if @contratos_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contratos_plan.errors.count, "error") %> prohibited this contratos_plan from being saved:</h2>

      <ul>
      <% @contratos_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :valorTotal %><br>
    <%= f.number_field :valorTotal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
